This folder contains all the Docker job descriptions.
This Docker containers are structured as jobs so once
they are executed they left the result where indicated
and then die. Inside each folder you will find the
instructions to run the corresponding job.
